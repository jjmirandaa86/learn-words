name: CI/CD Deploy English word JJMA FrontEnd / BackEnd

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      packages: write

    env:
      IMAGE: ghcr.io/jjmirandaa86/learn-words:latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Build and Push Docker Image
        run: |
          docker build -t $IMAGE .
          docker push $IMAGE

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

      - name: Sync .env from GitHub secret to server
        env:
          LEARN_WORDS_ENV: ${{ secrets.LEARN_WORDS_ENV }}
        run: |
          if [ -z "$LEARN_WORDS_ENV" ]; then
            echo "❌ Falta el secret LEARN_WORDS_ENV"
            echo "   GitHub → Settings → Secrets → Actions → New repository secret"
            echo "   Pega el contenido completo de tu archivo .env local"
            exit 1
          fi
          printf '%s\n' "$LEARN_WORDS_ENV" > /tmp/learn-words.env
          ssh -i ~/.ssh/id_rsa root@${{ secrets.SERVER_IP }} "mkdir -p /app/projects"
          scp -i ~/.ssh/id_rsa /tmp/learn-words.env root@${{ secrets.SERVER_IP }}:/app/projects/.env
          ssh -i ~/.ssh/id_rsa root@${{ secrets.SERVER_IP }} "chmod 600 /app/projects/.env"
          rm -f /tmp/learn-words.env
          echo "✅ .env sincronizado en el servidor"

      - name: Deploy to Server
        run: |
          ssh -i ~/.ssh/id_rsa root@${{ secrets.SERVER_IP }} << EOF
            cd /app/projects || exit 1

            set -e

            IMAGE=ghcr.io/jjmirandaa86/learn-words:latest
            ENV_FILE=/app/projects/.env

            echo "🔐 Login to GHCR from droplet..."
            echo "${{ secrets.GHCR_TOKEN }}" | docker login ghcr.io -u jjmirandaa86 --password-stdin

            echo "🌐 Creating Docker network if not exists..."
            docker network inspect app_network >/dev/null 2>&1 || docker network create app_network

            echo "📦 Pulling image..."
            docker pull \$IMAGE

            echo "🛑 Stopping old container..."
            docker stop learn-words || true
            docker rm learn-words || true

            echo "🚀 Running new container..."
            docker run -d \
              --name learn-words \
              --network app_network \
              --restart unless-stopped \
              --env-file "\$ENV_FILE" \
              \$IMAGE

            echo "✅ Deploy completed"
          EOF
